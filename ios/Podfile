require File.expand_path(File.join('..', '.dart_tool', 'flutter_build', 'podhelper.rb'), __FILE__)

platform :ios, '14.0'
use_modular_headers!

target 'Runner' do
  flutter_install_all_ios_pods File.dirname(File.realpath(__FILE__))
end
